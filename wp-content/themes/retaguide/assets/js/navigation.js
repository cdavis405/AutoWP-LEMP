(function(){const toggles=document.querySelectorAll('[data-retaguide-nav-toggle]');toggles.forEach(toggle=>{const targetId=toggle.getAttribute('data-retaguide-nav-toggle');const target=document.getElementById(targetId);if(!target){return;}toggle.addEventListener('click',()=>{const expanded=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!expanded));target.classList.toggle('is-open');});});})();
